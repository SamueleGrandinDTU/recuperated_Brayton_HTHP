# tests/

Automated tests

This directory contains automated tests for the codebase:
- Unit tests for individual functions and classes
- Integration tests for workflows
- Validation tests against known solutions
- Performance benchmarks

Tests should mirror the structure of `src/` and use a consistent testing framework (e.g., pytest).

